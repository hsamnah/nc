namespace nc
{
    internal class Enumerators
    {
        // venue marketing
        public enum VM : int { EmailBroadcasting = 1, BannerPublishing = 2, FrontendWebsite = 3, WebSitePositioning = 4, MarketResearch = 5 };

        // Event promotions
        public enum EP : int { photogallery = 1, EmailBroadcasting = 2, ticketSales = 3, Reservations = 4, EventCalendar = 5 };

        // Customer relations
        public enum CR : int { RealTimeChat = 1, EmailBroadcasting = 2, newsreleases = 3 };

        // Recruiting/staffing
        public enum RS : int { jobBoard = 1, EventScheduling = 2, Communication = 3 };
    }
}